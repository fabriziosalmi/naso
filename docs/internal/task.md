# Task List: Fase 5 God-Tier (UX & Telemetry)

## 1. Bug Tracking Universale (Sentry)
- [x] Add `sentry-sdk[fastapi]` to `backend/requirements.txt` and configure in `main.py`.
- [x] Add `@sentry/react` and `@sentry/tracing` to frontend `package.json`.

## 2. In-App Docs (CMDK Palette)
- [x] Install `cmdk` in frontend.
- [x] Create `frontend/src/components/ui/CommandMenu.jsx` to implement CMD+K Spotlight search.
- [x] Inject `CommandMenu` globally in `App.jsx`.

## 3. Onboarding Infallibile (UX)
- [x] Install `react-joyride` and `@radix-ui/react-tooltip`.
- [x] Create `frontend/src/components/layout/OnboardingTour.jsx`.
- [x] Apply semantic tooltips to `frontend/src/components/layout/Sidebar.jsx`.

## 4. Verification & Walkthrough
- [x] Verify codebase builds successfully.
- [x] Write final `walkthrough.md`.
