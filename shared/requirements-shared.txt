# Dependencies shared by the API image and the worker images.
# Anything imported from `shared/` belongs here, not in one of the callers.

sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
pydantic[email]>=2.5.3
pydantic-settings>=2.5.2
# python-dotenv is not optional: shared/config.py sets env_file=".env", and
# pydantic-settings raises on import without it.
python-dotenv>=1.2.2

# shared/core/security.py imports `jwt` (PyJWT) and hashes with pbkdf2_sha256,
# which is built into passlib — there is no extra to request for it. The
# previous `passlib[pbkdf2_sha256]` spelling was not a real extra and pip
# warned about it on every build.
PyJWT[crypto]>=2.8.0
passlib==1.7.4

minio==7.2.20
elasticsearch[async]==8.19.3
httpx>=0.27.0
tenacity==8.5.0

opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp
