fastapi>=0.111.0
# uvicorn floor is dictated by mcp>=1.23.0, which requires uvicorn>=0.31.1
# (CVE-2025-66416 in mcp). Kept as a >= so future minor bumps can flow.
uvicorn[standard]>=0.31.1,<1
uvloop==0.19.0
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
httpx>=0.27.0
elasticsearch[async]==8.12.0
pydantic[email]>=2.5.3
pydantic-settings>=2.5.2
PyJWT[crypto]>=2.8.0
passlib[bcrypt]==1.7.4
python-multipart>=0.0.18
alembic==1.13.1
minio==7.2.3
celery
fpdf2==2.7.7
opentelemetry-api
opentelemetry-sdk
opentelemetry-instrumentation-fastapi
opentelemetry-instrumentation-sqlalchemy
opentelemetry-exporter-otlp

pika==1.3.2
redis>=5.0.0
pip-audit
rich
ruff
slowapi>=0.1.9
aio-pika>=9.3.0
orjson>=3.9.15
# Used by shared.domain.services.dark_web.ahmia_client (HTML parsing)
# and imported transitively by several test modules at collection time.
# Mirrors the workers/requirements.txt pin.
beautifulsoup4>=4.12
# stem: Tor control-port client used by shared.domain.services.dark_web.tor_control
# to issue NEWNYM circuit-rotation signals. Imported lazily so clearnet-only
# deployments don't pay the import cost, but required when
# NASO_DARKWEB_ROTATE_CIRCUIT is on.
stem>=1.8.2
bandit
tenacity==8.2.3
pytest
pytest-asyncio
pytest-cov
# Per-test wall-clock cap so a single hanging test can't pin the whole
# CI job. Honors the ``timeout`` marker registered in pyproject.toml
# and ``--timeout=N`` from the CLI.
pytest-timeout
# In-memory SQLite driver used by the test conftest. Not needed at
# runtime but the test suite imports it at collection time, so it
# has to live alongside pytest in this list.
aiosqlite>=0.19.0
-r shared/requirements-shared.txt
# CVE-2025-66416 fixed in 1.23.0; pip-audit gates on this floor.
mcp>=1.23.0
sentry-sdk[fastapi]>=1.40.4
